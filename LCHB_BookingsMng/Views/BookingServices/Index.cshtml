@model IEnumerable<LCHB_BookingsMng.Models.BookingService>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BookingSVDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Request)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomNav)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomServiceNav)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BookingSVDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Request)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNav.RoomName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomServiceNav.RoomServiceName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BookingServiceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BookingServiceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BookingServiceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
