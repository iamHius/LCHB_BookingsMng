@model IEnumerable<LCHB_BookingsMng.Models.RoomService>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoomServiceName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoomServicePrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomServiceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomServicePrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoomServiceId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoomServiceId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoomServiceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
