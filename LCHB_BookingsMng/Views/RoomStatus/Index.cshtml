@model IEnumerable<LCHB_BookingsMng.Models.RoomStatus>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RoomStatusName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoomStatusName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoomStatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoomStatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoomStatusId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
